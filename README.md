# Sales-Dashboard
```bash
# Step 1: Clone the repository
gitclone https://github.com/Lahari-Nandigani/Sales-Dashboard.git

# Step 2: Enter into the folder
cd Sales-Dashboard

# Step 3: Install Streamlit using command prompt (cmd) 
pip install Streamlit

# Step 4: Install Pandas using comamnd prompt (cmd)
pip install pandas

# Step 5: Install Plotly using comamnd prompt (cmd)
pip install plotly

# Step 5: Run the program in the command prompt (cmd)
streamlit run dashboards.py
```
